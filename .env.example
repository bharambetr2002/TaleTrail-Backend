# TaleTrail API - Example Environment Variables

# --- Supabase Credentials ---
SUPABASE_URL=https://your-supabase-project-url.supabase.co
SUPABASE_KEY=your-supabase-service-role-key
SUPABASE_JWT_SECRET=your-supabase-jwt-secret

# --- CORS Allowed Origins (comma-separated, e.g. http://localhost:3000,https://yourfrontend.com) ---
ALLOWED_ORIGINS=http://localhost:3000

# --- ASP.NET Core Environment ---
ASPNETCORE_ENVIRONMENT=Development

# --- Swagger (set to true to enable in production) ---
ENABLE_SWAGGER=true

# --- Port (used by some platforms like Render) ---
PORT=8080

# Add any other environment variables as needed